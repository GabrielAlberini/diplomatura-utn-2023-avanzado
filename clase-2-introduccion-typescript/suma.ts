// Todos los ejercicios deben resolverse con una función y TODAS deben retornar el resultado, no mostrarlo en consola.

const operacionSumar = (n1: number, n2:number ): number => {
  let resultado: number = n1 + n2
  return resultado
}

const resultadoDeSuma = operacionSumar(1, 2)
console.log(resultadoDeSuma)

